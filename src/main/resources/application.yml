spring:
  datasource:
    url: 'jdbc:h2:mem:testdb;Mode=Oracle'
    type: com.zaxxer.hikari.HikariDataSource
    data: classpath:some_offenders.sql,classpath:some_offender_identifiers.sql,classpath:some_offender_bookings.sql


  h2:
    console:
      enabled: true
  jpa:
#    hibernate:
#      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        default_schema: 'PUBLIC'

