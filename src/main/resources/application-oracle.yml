spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.Oracle10gDialect
        show_sql: false
        jdbc:
          fetch_size: 100
  profiles:
    include: logstash